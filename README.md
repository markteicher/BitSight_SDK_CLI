
🛡️ BitSight SDK + CLI

## Overview

The BitSight SDK + CLI is a production-grade command-line interface and Python SDK designed to extract, normalize, and persist the full BitSight API surface into enterprise databases for operational intelligence, analytics, and downstream platforms.

It provides complete API coverage, deterministic ingestion, and a unified control surface for managing BitSight data at scale.

⸻

✨ Key Capabilities

🧠 Core Platform
